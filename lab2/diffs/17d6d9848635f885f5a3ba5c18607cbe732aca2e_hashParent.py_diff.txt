@@ -13,6 +13,7 @@ class HashParent:
     def decrypt(self, text):
         logger.debug(f"Calling hash crackers")
         result = hashBuster.crack(text, self.lc)
+        logger.debug(f"Returning {result}")
         return result
 
     def setProbTable(self, val):
