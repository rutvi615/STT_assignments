@@ -3,6 +3,7 @@ on: [push, pull_request]
 jobs:
   tests:
     strategy:
+      fail-fast: false
       matrix:
         python-version: ['3.7', '3.8']
         os: [ubuntu-latest, macos-latest, windows-latest]
